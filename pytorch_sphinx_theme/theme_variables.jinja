{%- set shared_menu_en = [                                                                            
    ]
-%}

{%- set shared_menu_cn = [
    ]
-%}

{%- set lang_menu_mapping = {'default': shared_menu_en, 'en': shared_menu_en} -%}
